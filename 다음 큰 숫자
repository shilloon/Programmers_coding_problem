#include <string>
#include <vector>
#include <iostream>

using namespace std;

int solution(int n) {
    int target_ones = __builtin_popcount(n);
    
    int next_num = n + 1;
    while(__builtin_popcount(next_num) != target_ones)
    {
        next_num++;
    }
    
    return next_num;
}
